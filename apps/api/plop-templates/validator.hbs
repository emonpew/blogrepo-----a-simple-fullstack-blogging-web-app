// src/validators/{{name}}.validator.ts
import { body } from 'express-validator';

export const {{name}}Validator = {
  create: [
    body('name').notEmpty().withMessage('Name is required'),
    body('email').isEmail().withMessage('Invalid email'),
  ],
};